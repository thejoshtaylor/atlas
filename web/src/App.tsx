// Placeholder root component. The route tree, the fetch seam, and the
// shared shell (header, navigation, the authenticated-route guard, and
// the setup-incomplete gate) are this plan's Task 2 -- this task's scope
// is the scaffold and the design system it builds on.
function App() {
  return (
    <main className="flex min-h-svh items-center justify-center p-6">
      <p className="text-body text-muted-foreground">spire-voice</p>
    </main>
  )
}

export default App
