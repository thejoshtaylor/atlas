apiVersion: v1
kind: Service
metadata:
  name: {{ include "spire-voice.fullname" . }}
  labels:
    {{- include "spire-voice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "spire-voice.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
